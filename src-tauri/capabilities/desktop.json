{
  "identifier": "desktop",
  "description": "Capability for desktop only (Windows/macOS/Linux)",
  "windows": ["main"],
  "platforms": ["windows", "macOS", "linux"],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
